export enum OrderStatus {
  PROCESSING = "PROCESSING",
  COMPLETED = "COMPLETED",
}

export enum StockStatus {
  IN_STOCK = "IN_STOCK",
  SOLD = "SOLD",
}

export enum LogLevel {
  ERROR = "error",
  WARN = "warn",
  INFO = "info",
}
